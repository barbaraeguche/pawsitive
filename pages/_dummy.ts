// i added this file so that git and vercel could recognize this folder.
// this folder had to be added so that the auth.config.ts page authorizations could be recognized.
export default function Dummy() {
	return null;
}